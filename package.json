{
  "name": "push-graph-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service sainytk/shout-protocol",
    "create-local": "graph create --node http://localhost:8020/ sainytk/shout-protocol",
    "remove-local": "graph remove --node http://localhost:8020/ sainytk/shout-protocol",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sainytk/shout-protocol",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.43.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2"
  }
}
